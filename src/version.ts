/**
 * The package version. Replaced by a build step before publish via Rollup's
 * `replace` plugin — but kept as a hard-coded constant here so editor tooling
 * (and tests) sees a literal string.
 */
export const VERSION = '0.1.0';
